max_INCTAX = max(INCTAX),
mean_Log_INCTAX = mean(Log_INCTAX),
median_Log_INCTAX = median(Log_INCTAX),
sd_Log_INCTAX = sd(Log_INCTAX),
min_Log_INCTAX = min(Log_INCTAX),
max_Log_INCTAX = max(Log_INCTAX)
)
# Combine the summary statistics into a single dataframe
summary_df <- bind_rows(
data.frame(Group = "c", summary_c),
data.frame(Group = "t", summary_t),
data.frame(Group = "Overall", summary_overall)
)
# Print the summary dataframe
print(summary_df)
# Calculate summary statistics for group 'c'
summary_c_CORPINCTX <- Rev %>%
filter(group == "c") %>%
summarize(
n = n(),
mean_CORPINCTX = mean(CORPINCTX),
median_CORPINCTX = median(CORPINCTX),
sd_CORPINCTX = sd(CORPINCTX),
min_CORPINCTX = min(CORPINCTX),
max_CORPINCTX = max(CORPINCTX)
)
# Calculate summary statistics for group 't'
summary_t_CORPINCTX <- Rev %>%
filter(group == "t") %>%
summarize(
n = n(),
mean_CORPINCTX = mean(CORPINCTX),
median_CORPINCTX = median(CORPINCTX),
sd_CORPINCTX = sd(CORPINCTX),
min_CORPINCTX = min(CORPINCTX),
max_CORPINCTX = max(CORPINCTX)
)
# Calculate summary statistics for all observations
summary_overall_CORPINCTX <- Rev %>%
summarize(
n = n(),
mean_CORPINCTX = mean(CORPINCTX),
median_CORPINCTX = median(CORPINCTX),
sd_CORPINCTX = sd(CORPINCTX),
min_CORPINCTX = min(CORPINCTX),
max_CORPINCTX = max(CORPINCTX)
)
# Combine the summary statistics into a single dataframe for 'CORPINCTX'
summary_df_CORPINCTX <- bind_rows(
data.frame(Group = "c", summary_c_CORPINCTX),
data.frame(Group = "t", summary_t_CORPINCTX),
data.frame(Group = "Overall", summary_overall_CORPINCTX)
)
# Repeat the process for other columns
# Summary statistics for 'Log_CORPINCTX'
summary_c_Log_CORPINCTX <- Rev %>%
filter(group == "c") %>%
summarize(
n = n(),
mean_Log_CORPINCTX = mean(Log_CORPINCTX),
median_Log_CORPINCTX = median(Log_CORPINCTX),
sd_Log_CORPINCTX = sd(Log_CORPINCTX),
min_Log_CORPINCTX = min(Log_CORPINCTX),
max_Log_CORPINCTX = max(Log_CORPINCTX)
)
summary_t_Log_CORPINCTX <- Rev %>%
filter(group == "t") %>%
summarize(
n = n(),
mean_Log_CORPINCTX = mean(Log_CORPINCTX),
median_Log_CORPINCTX = median(Log_CORPINCTX),
sd_Log_CORPINCTX = sd(Log_CORPINCTX),
min_Log_CORPINCTX = min(Log_CORPINCTX),
max_Log_CORPINCTX = max(Log_CORPINCTX)
)
summary_overall_Log_CORPINCTX <- Rev %>%
summarize(
n = n(),
mean_Log_CORPINCTX = mean(Log_CORPINCTX),
median_Log_CORPINCTX = median(Log_CORPINCTX),
sd_Log_CORPINCTX = sd(Log_CORPINCTX),
min_Log_CORPINCTX = min(Log_CORPINCTX),
max_Log_CORPINCTX = max(Log_CORPINCTX)
)
summary_df_Log_CORPINCTX <- bind_rows(
data.frame(Group = "c", summary_c_Log_CORPINCTX),
data.frame(Group = "t", summary_t_Log_CORPINCTX),
data.frame(Group = "Overall", summary_overall_Log_CORPINCTX)
)
# Summary statistics for 'INCTAX'
summary_c_INCTAX <- Rev %>%
filter(group == "c") %>%
summarize(
n = n(),
mean_INCTAX = mean(INCTAX),
median_INCTAX = median(INCTAX),
sd_INCTAX = sd(INCTAX),
min_INCTAX = min(INCTAX),
max_INCTAX = max(INCTAX)
)
summary_t_INCTAX <- Rev %>%
filter(group == "t") %>%
summarize(
n = n(),
mean_INCTAX = mean(INCTAX),
median_INCTAX = median(INCTAX),
sd_INCTAX = sd(INCTAX),
min_INCTAX = min(INCTAX),
max_INCTAX = max(INCTAX)
)
summary_overall_INCTAX <- Rev %>%
summarize(
n = n(),
mean_INCTAX = mean(INCTAX),
median_INCTAX = median(INCTAX),
sd_INCTAX = sd(INCTAX),
min_INCTAX = min(INCTAX),
max_INCTAX = max(INCTAX)
)
summary_df_INCTAX <- bind_rows(
data.frame(Group = "c", summary_c_INCTAX),
data.frame(Group = "t", summary_t_INCTAX),
data.frame(Group = "Overall", summary_overall_INCTAX)
)
# Summary statistics for 'Log_INCTAX'
summary_c_Log_INCTAX <- Rev %>%
filter(group == "c") %>%
summarize(
n = n(),
mean_Log_INCTAX = mean(Log_INCTAX),
median_Log_INCTAX = median(Log_INCTAX),
sd_Log_INCTAX = sd(Log_INCTAX),
min_Log_INCTAX = min(Log_INCTAX),
max_Log_INCTAX = max(Log_INCTAX)
)
summary_t_Log_INCTAX <- Rev %>%
filter(group == "t") %>%
summarize(
n = n(),
mean_Log_INCTAX = mean(Log_INCTAX),
median_Log_INCTAX = median(Log_INCTAX),
sd_Log_INCTAX = sd(Log_INCTAX),
min_Log_INCTAX = min(Log_INCTAX),
max_Log_INCTAX = max(Log_INCTAX)
)
summary_overall_Log_INCTAX <- Rev %>%
summarize(
n = n(),
mean_Log_INCTAX = mean(Log_INCTAX),
median_Log_INCTAX = median(Log_INCTAX),
sd_Log_INCTAX = sd(Log_INCTAX),
min_Log_INCTAX = min(Log_INCTAX),
max_Log_INCTAX = max(Log_INCTAX)
)
summary_df_Log_INCTAX <- bind_rows(
data.frame(Group = "c", summary_c_Log_INCTAX),
data.frame(Group = "t", summary_t_Log_INCTAX),
data.frame(Group = "Overall", summary_overall_Log_INCTAX)
)
# Print the summary dataframes
print(summary_df_CORPINCTX)
print(summary_df_Log_CORPINCTX)
print(summary_df_INCTAX)
print(summary_df_Log_INCTAX)
# Filter out rows with NA and zero values for 'CORPINCTX'
Rev_filtered_CORPINCTX <- Rev %>%
filter(!is.na(CORPINCTX) & CORPINCTX != 0)
# Calculate summary statistics for group 'c' after filtering
summary_c_CORPINCTX <- Rev_filtered_CORPINCTX %>%
filter(group == "c") %>%
summarize(
n = n(),
mean_CORPINCTX = mean(CORPINCTX),
median_CORPINCTX = median(CORPINCTX),
sd_CORPINCTX = sd(CORPINCTX),
min_CORPINCTX = min(CORPINCTX),
max_CORPINCTX = max(CORPINCTX)
)
# Repeat the process for other columns
# Filter out rows with NA and zero values for 'Log_CORPINCTX'
Rev_filtered_Log_CORPINCTX <- Rev %>%
filter(!is.na(Log_CORPINCTX) & Log_CORPINCTX != 0)
# Calculate summary statistics for group 'c' after filtering
summary_c_Log_CORPINCTX <- Rev_filtered_Log_CORPINCTX %>%
filter(group == "c") %>%
summarize(
n = n(),
mean_Log_CORPINCTX = mean(Log_CORPINCTX),
median_Log_CORPINCTX = median(Log_CORPINCTX),
sd_Log_CORPINCTX = sd(Log_CORPINCTX),
min_Log_CORPINCTX = min(Log_CORPINCTX),
max_Log_CORPINCTX = max(Log_CORPINCTX)
)
# Repeat the process for other columns
# Filter out rows with NA and zero values for 'INCTAX'
Rev_filtered_INCTAX <- Rev %>%
filter(!is.na(INCTAX) & INCTAX != 0)
# Calculate summary statistics for group 'c' after filtering
summary_c_INCTAX <- Rev_filtered_INCTAX %>%
filter(group == "c") %>%
summarize(
n = n(),
mean_INCTAX = mean(INCTAX),
median_INCTAX = median(INCTAX),
sd_INCTAX = sd(INCTAX),
min_INCTAX = min(INCTAX),
max_INCTAX = max(INCTAX)
)
# Repeat the process for other columns
# Filter out rows with NA and zero values for 'Log_INCTAX'
Rev_filtered_Log_INCTAX <- Rev %>%
filter(!is.na(Log_INCTAX) & Log_INCTAX != 0)
# Calculate summary statistics for group 'c' after filtering
summary_c_Log_INCTAX <- Rev_filtered_Log_INCTAX %>%
filter(group == "c") %>%
summarize(
n = n(),
mean_Log_INCTAX = mean(Log_INCTAX),
median_Log_INCTAX = median(Log_INCTAX),
sd_Log_INCTAX = sd(Log_INCTAX),
min_Log_INCTAX = min(Log_INCTAX),
max_Log_INCTAX = max(Log_INCTAX)
)
# Print the summary dataframes
print(summary_c_CORPINCTX)
print(summary_c_Log_CORPINCTX)
print(summary_c_INCTAX)
print(summary_c_Log_INCTAX)
# Assuming 'Rev' is your dataframe containing the groups 'c' and 't'
# Replace NAs with zeros for 'CORPINCTX'
Rev$CORPINCTX <- replace(Rev$CORPINCTX, is.na(Rev$CORPINCTX), 0)
# Calculate summary statistics for group 'c'
summary_c_CORPINCTX <- Rev %>%
filter(group == "c") %>%
summarize(
n = n(),
mean_CORPINCTX = mean(CORPINCTX),
median_CORPINCTX = median(CORPINCTX),
sd_CORPINCTX = sd(CORPINCTX),
min_CORPINCTX = min(CORPINCTX),
max_CORPINCTX = max(CORPINCTX)
)
# Repeat the process for other columns
# Replace NAs with zeros for 'Log_CORPINCTX'
Rev$Log_CORPINCTX <- replace(Rev$Log_CORPINCTX, is.na(Rev$Log_CORPINCTX), 0)
# Calculate summary statistics for group 'c'
summary_c_Log_CORPINCTX <- Rev %>%
filter(group == "c") %>%
summarize(
n = n(),
mean_Log_CORPINCTX = mean(Log_CORPINCTX),
median_Log_CORPINCTX = median(Log_CORPINCTX),
sd_Log_CORPINCTX = sd(Log_CORPINCTX),
min_Log_CORPINCTX = min(Log_CORPINCTX),
max_Log_CORPINCTX = max(Log_CORPINCTX)
)
# Repeat the process for other columns
# Replace NAs with zeros for 'INCTAX'
Rev$INCTAX <- replace(Rev$INCTAX, is.na(Rev$INCTAX), 0)
# Calculate summary statistics for group 'c'
summary_c_INCTAX <- Rev %>%
filter(group == "c") %>%
summarize(
n = n(),
mean_INCTAX = mean(INCTAX),
median_INCTAX = median(INCTAX),
sd_INCTAX = sd(INCTAX),
min_INCTAX = min(INCTAX),
max_INCTAX = max(INCTAX)
)
# Repeat the process for other columns
# Replace NAs with zeros for 'Log_INCTAX'
Rev$Log_INCTAX <- replace(Rev$Log_INCTAX, is.na(Rev$Log_INCTAX), 0)
# Calculate summary statistics for group 'c'
summary_c_Log_INCTAX <- Rev %>%
filter(group == "c") %>%
summarize(
n = n(),
mean_Log_INCTAX = mean(Log_INCTAX),
median_Log_INCTAX = median(Log_INCTAX),
sd_Log_INCTAX = sd(Log_INCTAX),
min_Log_INCTAX = min(Log_INCTAX),
max_Log_INCTAX = max(Log_INCTAX)
)
# Print the summary dataframes
print(summary_c_CORPINCTX)
print(summary_c_Log_CORPINCTX)
print(summary_c_INCTAX)
print(summary_c_Log_INCTAX)
# Replace NAs with zeros for 'CORPINCTX'
Rev$CORPINCTX <- replace(Rev$CORPINCTX, is.na(Rev$CORPINCTX), 0)
# Calculate summary statistics for group 'c'
summary_c_CORPINCTX <- Rev %>%
filter(group == "c") %>%
summarize(
n = n(),
mean_CORPINCTX = mean(CORPINCTX),
median_CORPINCTX = median(CORPINCTX),
sd_CORPINCTX = sd(CORPINCTX),
min_CORPINCTX = min(CORPINCTX),
max_CORPINCTX = max(CORPINCTX)
)
# Repeat the process for other columns
# Replace NAs with zeros for 'Log_CORPINCTX'
Rev$Log_CORPINCTX <- replace(Rev$Log_CORPINCTX, is.na(Rev$Log_CORPINCTX), 0)
# Calculate summary statistics for group 'c'
summary_c_Log_CORPINCTX <- Rev %>%
filter(group == "c") %>%
summarize(
n = n(),
mean_Log_CORPINCTX = mean(Log_CORPINCTX),
median_Log_CORPINCTX = median(Log_CORPINCTX),
sd_Log_CORPINCTX = sd(Log_CORPINCTX),
min_Log_CORPINCTX = min(Log_CORPINCTX),
max_Log_CORPINCTX = max(Log_CORPINCTX)
)
# Repeat the process for other columns
# Replace NAs with zeros for 'INCTAX'
Rev$INCTAX <- replace(Rev$INCTAX, is.na(Rev$INCTAX), 0)
# Calculate summary statistics for group 'c'
summary_c_INCTAX <- Rev %>%
filter(group == "c") %>%
summarize(
n = n(),
mean_INCTAX = mean(INCTAX),
median_INCTAX = median(INCTAX),
sd_INCTAX = sd(INCTAX),
min_INCTAX = min(INCTAX),
max_INCTAX = max(INCTAX)
)
# Repeat the process for other columns
# Replace NAs with zeros for 'Log_INCTAX'
Rev$Log_INCTAX <- replace(Rev$Log_INCTAX, is.na(Rev$Log_INCTAX), 0)
# Calculate summary statistics for group 'c'
summary_c_Log_INCTAX <- Rev %>%
filter(group == "c") %>%
summarize(
n = n(),
mean_Log_INCTAX = mean(Log_INCTAX),
median_Log_INCTAX = median(Log_INCTAX),
sd_Log_INCTAX = sd(Log_INCTAX),
min_Log_INCTAX = min(Log_INCTAX),
max_Log_INCTAX = max(Log_INCTAX)
)
# Print the summary dataframes
print(summary_c_CORPINCTX)
print(summary_c_Log_CORPINCTX)
print(summary_c_INCTAX)
print(summary_c_Log_INCTAX)
Rev[is.na(Rev)] <- 0
# Calculate summary statistics for group 'c' after replacing NAs with 0
summary_c_INCTAX <- Rev %>%
filter(group == "c") %>%
summarize(
n = n(),
mean_INCTAX = mean(INCTAX),
median_INCTAX = median(INCTAX),
sd_INCTAX = sd(INCTAX),
min_INCTAX = min(INCTAX),
max_INCTAX = max(INCTAX)
)
# Repeat the process for group 't'
summary_t_INCTAX <- Rev %>%
filter(group == "t") %>%
summarize(
n = n(),
mean_INCTAX = mean(INCTAX),
median_INCTAX = median(INCTAX),
sd_INCTAX = sd(INCTAX),
min_INCTAX = min(INCTAX),
max_INCTAX = max(INCTAX)
)
# Calculate summary statistics for all observations
summary_overall_INCTAX <- Rev %>%
summarize(
n = n(),
mean_INCTAX = mean(INCTAX),
median_INCTAX = median(INCTAX),
sd_INCTAX = sd(INCTAX),
min_INCTAX = min(INCTAX),
max_INCTAX = max(INCTAX)
)
# Combine the summary statistics into a single dataframe for 'INCTAX'
summary_df_INCTAX <- bind_rows(
data.frame(Group = "c", summary_c_INCTAX),
data.frame(Group = "t", summary_t_INCTAX),
data.frame(Group = "Overall", summary_overall_INCTAX)
)
# Print the summary dataframe for 'INCTAX'
print(summary_df_INCTAX)
Rev_filtered <- Rev %>%
filter(INCTAX != 0)
View(Rev_filtered)
Rev[is.na(Rev)] <- 0
View(Rev)
# Replace NA values with 0 in specific columns
Rev$INCTAX[is.na(Rev$INCTAX)] <- 0
Rev$Log_CORPINCTX[is.na(Rev$Log_CORPINCTX)] <- 0
Rev$Log_INCTAX[is.na(Rev$Log_INCTAX)] <- 0
# Calculate summary statistics for group 'c' after replacing NAs with 0
summary_c_INCTAX <- Rev %>%
filter(group == "c") %>%
summarize(
n = n(),
mean_INCTAX = mean(INCTAX),
median_INCTAX = median(INCTAX),
sd_INCTAX = sd(INCTAX),
min_INCTAX = min(INCTAX),
max_INCTAX = max(INCTAX)
)
# Repeat the process for group 't'
summary_t_INCTAX <- Rev %>%
filter(group == "t") %>%
summarize(
n = n(),
mean_INCTAX = mean(INCTAX),
median_INCTAX = median(INCTAX),
sd_INCTAX = sd(INCTAX),
min_INCTAX = min(INCTAX),
max_INCTAX = max(INCTAX)
)
# Calculate summary statistics for all observations
summary_overall_INCTAX <- Rev %>%
summarize(
n = n(),
mean_INCTAX = mean(INCTAX),
median_INCTAX = median(INCTAX),
sd_INCTAX = sd(INCTAX),
min_INCTAX = min(INCTAX),
max_INCTAX = max(INCTAX)
)
# Combine the summary statistics into a single dataframe for 'INCTAX'
summary_df_INCTAX <- bind_rows(
data.frame(Group = "c", summary_c_INCTAX),
data.frame(Group = "t", summary_t_INCTAX),
data.frame(Group = "Overall", summary_overall_INCTAX)
)
# Print the summary dataframe for 'INCTAX'
print(summary_df_INCTAX)
Rev$INCTAX[is.na(Rev$INCTAX)] <- 0
View(Rev)
View(Rev)
Rev2 <- Rev$INCTAX[is.na(Rev$INCTAX)] <- 0
Rev2 <- Rev%>% Rev$INCTAX[is.na(Rev$INCTAX)] <- 0
Rev2 <- Rev$INCTAX[is.na(Rev$INCTAX)] <- 0
Rev$INCTAX[is.na(Rev$INCTAX)] <- 0
Rev2 <- Rev %>%
mutate(
INCTAX = replace(INCTAX, is.na(INCTAX), 0),
Log_CORPINCTX = replace(Log_CORPINCTX, is.na(Log_CORPINCTX), 0),
Log_INCTAX = replace(Log_INCTAX, is.na(Log_INCTAX), 0)
)
View(Rev2)
Rev$INCTAX <- ifelse(is.na(Rev$INCTAX), 0, Rev$INCTAX)
View(Rev)
Rev$INCTAX <- gsub("NA", "0", Rev$INCTAX)
View(Rev)
Rev$Log_CORPINCTX <- gsub("NA", "0", Rev$Log_CORPINCTX)
Rev$Log_INCTAX <- gsub("NA", "0", Rev$Log_INCTAX)
Rev_filtered <- Rev[Rev$INCTAX != 0, ]
summary_stats_INCTAX <- Rev_filtered %>%
summarize(
n = n(),
mean = mean(INCTAX, na.rm = TRUE),
median = median(INCTAX, na.rm = TRUE),
sd = sd(INCTAX, na.rm = TRUE),
min = min(INCTAX, na.rm = TRUE),
max = max(INCTAX, na.rm = TRUE)
)
print(summary_stats_INCTAX)
View(Rev_filtered)
write.csv(Rev_filtered,"SummaryStats_Sheet_4_6_24.csv")
# Calculate summary statistics for group 'c' for the INCTAX column
summary_c_INCTAX <- Rev_filtered %>%
filter(group == "c") %>%
summarize(
n_INCTAX = n(),
mean_INCTAX_c = mean(INCTAX),
median_INCTAX_c = median(INCTAX),
sd_INCTAX_c = sd(INCTAX),
min_INCTAX_c = min(INCTAX),
max_INCTAX_c = max(INCTAX)
)
# Calculate summary statistics for group 't' for the INCTAX column
summary_t_INCTAX <- Rev_filtered %>%
filter(group == "t") %>%
summarize(
n_INCTAX = n(),
mean_INCTAX_t = mean(INCTAX),
median_INCTAX_t = median(INCTAX),
sd_INCTAX_t = sd(INCTAX),
min_INCTAX_t = min(INCTAX),
max_INCTAX_t = max(INCTAX)
)
# Calculate summary statistics for all observations for the INCTAX column
summary_overall_INCTAX <- Rev_filtered %>%
summarize(
n_INCTAX = n(),
mean_INCTAX = mean(INCTAX),
median_INCTAX = median(INCTAX),
sd_INCTAX = sd(INCTAX),
min_INCTAX = min(INCTAX),
max_INCTAX = max(INCTAX)
)
# Combine the summary statistics into a single dataframe for the INCTAX column
summary_df_INCTAX <- bind_rows(
data.frame(Group = "c", summary_c_INCTAX),
data.frame(Group = "t", summary_t_INCTAX),
data.frame(Group = "Overall", summary_overall_INCTAX)
)
# Print the summary dataframe for the INCTAX column
print(summary_df_INCTAX)
